<Window x:Class="CsharpTodolist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CsharpTodolist"
        mc:Ignorable="d"
        Title="MainWindow" Margin="10,10,0,13" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Column="0" Grid.Row="0" Text="Kekek">
        </TextBox>
        <Button Grid.Column="1">
            <TextBlock Text="Add new TODO item" />
        </Button>
        <GroupBox Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" BorderThickness="3" BorderBrush="Teal">
            <ListBox>
                <ListBoxItem>
                    <CheckBox>
                        <TextBlock Text="Something"></TextBlock>
                    </CheckBox>
                </ListBoxItem>
            </ListBox>
        </GroupBox>
    </Grid>
</Window>
